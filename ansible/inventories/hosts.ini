# Control-node local test target
[local]
localhost ansible_connection=local

# Temporary hypervisor (Parrot) - fill in later
[parrot]
# parrot ansible_host=192.168.1.50 ansible_user=<your_parrot_user>

# Final headless Proxmox - fill in later
[proxmox]
# proxmox ansible_host=192.168.1.60 ansible_user=root

[all:vars]
ansible_ssh_common_args='-o StrictHostKeyChecking=accept-new'
